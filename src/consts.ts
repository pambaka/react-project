export const SEARCH_VALUE = 'searchValue';

const BASE_URL = 'https://swapi.dev/api';

export const URLS = {
  people: `${BASE_URL}/people`,
};

export const PARAM = {
  search: 'search',
  page: 'page',
};
